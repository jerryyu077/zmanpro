# 本地财务计算系统 - 设计方案

**状态：✅ 设计完成，等待开发**  
**更新时间：2025-11-26**

---

## 🎯 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    🏠 主导航页 (Dashboard)                   │
│         本月概览：收入 | 支出 | 利润 + 功能导航               │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐      ┌──────────────┐     ┌──────────────┐
│  💼 员工管理  │      │  🏢 客户管理  │     │  📊 财务总结  │
│  (支出模块)   │      │  (收入模块)   │     │  (统计分析)   │
│  [🔄 同步]    │      │  (纯本地)     │     │  (云端+本地)  │
└──────────────┘      └──────────────┘     └──────────────┘
        │                     │                     │
    ┌───┴───┐                 │                     │
    │       │                 │                     │
    ▼       ▼                 ▼                     ▼
┌──────┐ ┌────────┐   ┌──────────────┐   ┌─────────────────┐
│员工   │ │工时记录│   │客户项目管理   │   │ • 按时间段统计  │
│基础   │ │管理    │   │（多计费段）   │   │ • 按员工统计    │
│信息   │ │(列表)  │   │（节假日费率） │   │ • 按客户统计    │
│☁️+💻 │ │☁️+💻  │   │💻 本地数据    │   │ • 云端+本地合计 │
└──────┘ └────────┘   └──────────────┘   └─────────────────┘
    │         │
    │         └─────────┐
    │                   │
    ▼                   ▼
┌─────────────────────────────────┐
│      🔄 云端数据同步模块          │
│                                 │
│  • 从 zmanpro API 拉取数据       │
│  • 二次确认（防止误操作）         │
│  • 完全覆盖云端数据              │
│  • 保留本地数据                  │
└─────────────────────────────────┘
         │                │
         │                │
    ┌────┴────┐      ┌────┴────┐
    ▼         ▼      ▼         ▼
┌─────────┐      ┌──────────────────┐
│ zmanpro │      │  localStorage     │
│   API   │      │  (本地浏览器存储)  │
│ (云端)  │      │                   │
│         │      │  • employees      │
│ Cloudflare     │  • work_records   │
│  Workers       │  • clients        │
│  + D1    │      │  • projects       │
└─────────┘      └──────────────────┘
```

### 📊 数据流说明

```
☁️ 云端数据流：
zmanpro API → [同步] → localStorage (标记 source: "cloud") → 只读显示

💻 本地数据流：
用户输入 → localStorage (标记 source: "local") → 可编辑

📈 财务计算流：
localStorage (云端 + 本地) → 合并计算 → 财务报表
```

---

## 📋 项目概述

### 核心需求
1. **完全本地运行** - 纯前端，无需服务器/API
2. **数据本地存储** - 使用浏览器 localStorage
3. **零配置启动** - 直接双击 HTML 文件即可使用，无需 npm/node

### 业务逻辑
- **支出部分**：员工工时 × 时薪 = 公司支出
- **收入部分**：客户项目 × 计费单价 = 公司收入
- **利润分析**：选择时间段，自动计算 收入 - 支出 = 利润

---

## ✅ 可行性分析

### 技术可行性：**完全可行** ✅

| 需求 | 解决方案 | 可行性 |
|------|---------|--------|
| 本地运行 | 纯 HTML + CSS + JS，无需服务器 | ✅ 完全可行 |
| 数据存储 | localStorage（浏览器原生支持，可存储 5-10MB） | ✅ 完全可行 |
| 零配置 | 不依赖任何构建工具或包管理器 | ✅ 完全可行 |
| 数据导出 | JSON 导出/导入功能（备份） | ✅ 完全可行 |
| 多设备同步 | ❌ 不支持（localStorage 仅限单设备单浏览器） | ⚠️ 需注意 |

### 优势
- ✅ 无需网络连接
- ✅ 数据私密，完全本地
- ✅ 启动速度快
- ✅ 维护简单，无部署成本

### 限制
- ⚠️ 数据仅存在单个浏览器中（清除浏览器数据会丢失）
- ⚠️ 无法多设备/多浏览器同步
- ⚠️ 需要手动备份数据（可提供导出功能）

---

## 🎯 系统架构

### 页面结构（6个主要页面）

```
📁 local-finance-system/
├── index.html                  # 1. 主导航页（Dashboard）
├── employee-management.html    # 2. 员工基础管理页
├── work-records.html           # 3. 员工工时管理页
├── clients.html                # 4. 客户管理页（含项目列表）
├── summary.html                # 5. 财务总结页（利润分析 + 统计）
├── data-management.html        # 6. 数据管理页（备份/导入）
├── css/
│   └── flat-style.css          # 统一样式（扁平风格）
└── js/
    ├── storage.js              # localStorage 封装（CRUD操作）
    ├── employee-mgmt.js        # 员工基础管理逻辑
    ├── work-records.js         # 工时记录管理逻辑
    ├── clients.js              # 客户管理逻辑
    ├── summary.js              # 财务总结逻辑
    ├── data-mgmt.js            # 数据备份/导入逻辑
    └── utils.js                # 工具函数（日期、计算、格式化等）
```

---

## 📊 数据结构设计

### 1. 员工数据（Employees）- 支出部分

**数据来源：云端同步 + 本地新增**

```javascript
// localStorage key: 'employees'
[
  {
    id: 1,                          // 本地 ID（自增）
    name: "张三",
    location: "北京",
    default_hourly_rate: 25,        // 默认时薪（美元）
    notes: "全职员工",
    
    // 新增：数据来源标记
    source: "cloud",                // "cloud" = 从 zmanpro 同步 | "local" = 本地新增
    cloud_id: 2,                    // 对应云端数据库的 employee.id（仅 cloud 来源有）
    synced_at: "2025-11-26T14:30:00Z", // 最后同步时间（仅 cloud 来源有）
    modified_locally: false,        // 是否在本地被修改过
    editable: true,                 // 是否可编辑（cloud 来源的可在本地修改，但不上传）
    
    created_at: "2025-11-26T10:00:00Z"
  },
  {
    id: 2,
    name: "李四",
    location: "上海",
    default_hourly_rate: 30,
    notes: "兼职设计师",
    
    source: "local",                // 本地新增的员工
    editable: true,
    
    created_at: "2025-11-26T15:00:00Z"
  }
]
```

### 2. 员工工时记录（Work Records）- 支出明细

**数据来源：云端同步（只读）+ 本地新增（可编辑）**

```javascript
// localStorage key: 'work_records'
[
  {
    id: 1,                        // 本地 ID（自增）
    employee_id: 1,               // 本地员工 ID
    employee_name: "张三",         // 冗余存储，方便列表显示
    date: "2025-12-01",           // 日期
    total_hours: 8,               // 工作小时数
    hourly_rate: 25,              // 该日时薪（美元）
    start_time: "09:00",          // 可选
    end_time: "18:00",            // 可选
    notes: "常规工作日",
    total_amount: 200,            // 自动计算：8 × 25 = $200
    
    // 新增：数据来源标记
    source: "cloud",              // "cloud" = 从 zmanpro 同步 | "local" = 本地新增
    cloud_id: 15,                 // 对应云端 work_records.id（仅 cloud 来源有）
    cloud_employee_id: 2,         // 对应云端 employee_id
    synced_at: "2025-11-26T14:30:00Z", // 同步时间
    editable: false,              // 云端数据只读，不可编辑
    display_style: "readonly",    // UI 显示样式：readonly = 灰色背景
    
    created_at: "2025-12-01T18:00:00Z"
  },
  {
    id: 2,
    employee_id: 1,
    employee_name: "张三",
    date: "2025-12-02",
    total_hours: 10,
    hourly_rate: 25,
    start_time: "09:00",
    end_time: "20:00",
    notes: "加班",
    total_amount: 250,            // 10 × 25 = $250
    
    source: "local",              // 本地新增
    editable: true,               // 可编辑
    display_style: "normal",      // 正常显示
    
    created_at: "2025-12-02T20:00:00Z"
  },
  {
    id: 3,
    employee_id: 2,               // 本地新增的员工
    employee_name: "李四",
    date: "2025-12-01",
    total_hours: 6,
    hourly_rate: 30,
    notes: "兼职工作",
    total_amount: 180,            // 6 × 30 = $180
    
    source: "local",
    editable: true,
    display_style: "normal",
    
    created_at: "2025-12-01T18:00:00Z"
  }
]
```

### 3. 客户/公司数据（Clients）- 收入部分

```javascript
// localStorage key: 'clients'
[
  {
    id: 1,
    name: "ABC公司",              // 客户/公司名称
    contact: "李经理",            // 联系人
    notes: "长期合作客户",
    created_at: "2025-11-26T10:00:00Z"
  }
]
```

### 4. 客户项目记录（Client Projects）- 收入明细

**核心设计：一个 period 可以有多个计费段（支持节假日特殊计费）**

```javascript
// localStorage key: 'client_projects'
[
  {
    id: 1,
    client_id: 1,
    client_name: "ABC公司",        // 冗余存储，方便显示
    project_name: "网站开发项目",  // 项目名称
    start_date: "2025-12-01",     // 整个项目的开始日期
    end_date: "2025-12-15",       // 整个项目的结束日期
    
    // 计费明细（支持 1-4 条，每条可以是不同的计费标准）
    billing_details: [
      {
        detail_id: 1,
        date_range: "12/1-12/10",  // 这段时间的日期范围（显示用）
        start_date: "2025-12-01",   // 开始日期
        end_date: "2025-12-10",     // 结束日期
        days_count: 10,             // 天数
        hours_per_day: 8,           // 每天小时数
        total_hours: 80,            // 小时数
        hourly_charge: 50,          // 计费单价（$/小时）
        is_holiday: false,          // 是否节假日费率
        subtotal: 4000,             // 小计（80 × 50 = $4,000）
        notes: "常规工作日"
      },
      {
        detail_id: 2,
        date_range: "12/11-12/15",
        start_date: "2025-12-11",
        end_date: "2025-12-15",
        days_count: 5,
        hours_per_day: 8,
        total_hours: 40,
        hourly_charge: 75,          // 节假日费率更高
        is_holiday: true,           // 节假日标记
        subtotal: 3000,             // 小计（40 × 75 = $3,000）
        notes: "节假日加价"
      }
    ],
    
    total_days: 15,               // 总天数（自动计算）
    total_hours: 120,             // 总小时数（自动计算）
    total_amount: 7000,           // 总金额（自动计算：$4,000 + $3,000）
    notes: "第一期开发",
    created_at: "2025-12-01T10:00:00Z"
  }
]
```

---

## 🎨 页面设计（UI/UX）

### 1️⃣ **主导航页（Dashboard）** - `index.html`

**功能：**
- 快速概览：本月收入、本月支出、本月利润
- 五个功能卡片导航按钮：
  - 📊 财务总结
  - 💼 员工基础管理
  - 📋 员工工时管理
  - 🏢 客户管理（收入）
  - 💾 数据备份/导入

**布局：**
```
┌─────────────────────────────────────────────────┐
│     本地财务计算系统                             │
├─────────────────────────────────────────────────┤
│  本月概览（2025年12月）                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 💰 收入   │ │ 💸 支出   │ │ 📈 利润   │        │
│  │ $9,500   │ │ $5,200   │ │ $4,300   │        │
│  │ 3个项目  │ │ 3个员工  │ │ 45.3%    │        │
│  └──────────┘ └──────────┘ └──────────┘        │
├─────────────────────────────────────────────────┤
│  功能导航                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │📊 财务总结│ │💼 员工管理│ │📋 工时管理│        │
│  └──────────┘ └──────────┘ └──────────┘        │
│  ┌──────────┐ ┌──────────┐                     │
│  │🏢 客户管理│ │💾 数据管理│                     │
│  └──────────┘ └──────────┘                     │
└─────────────────────────────────────────────────┘
```

---

### 2️⃣ **员工基础管理页（Employee Management）** - `employee-management.html`

**功能：管理员工基础信息 + 云端同步**

**页面布局：**
```
┌──────────────────────────────────────────────────────────┐
│ ← 返回        员工基础管理       [🔄 从 zmanpro 同步]    │
├──────────────────────────────────────────────────────────┤
│  🔍 搜索员工...                         ➕ 添加员工       │
│  数据来源筛选：[全部 ▼] [仅云端] [仅本地]                │
├──────────────────────────────────────────────────────────┤
│  员工列表（卡片形式，扁平风格）                           │
│  ┌────────────────────┐ ┌────────────────────┐         │
│  │ ☁️ 张三             │ │ 💻 李四             │         │
│  │ 地点：北京          │ │ 地点：上海          │         │
│  │ 默认时薪：$25/h     │ │ 默认时薪：$30/h     │         │
│  │ 备注：全职员工      │ │ 备注：兼职设计师    │         │
│  │ 同步：11/26 14:30   │ │ 本地创建：11/26     │         │
│  │ ✏️ 编辑  🗑️ 删除    │ │ ✏️ 编辑  🗑️ 删除    │         │
│  └────────────────────┘ └────────────────────┘         │
│                                                          │
│  说明：☁️ = 从 zmanpro 同步  |  💻 = 本地新增            │
└──────────────────────────────────────────────────────────┘
```

**🔄 同步按钮点击后的二次确认弹窗：**
```
┌──────────────────────────────────────────────────┐
│  ⚠️ 同步确认                                      │
├──────────────────────────────────────────────────┤
│  从 zmanpro 同步数据将会：                        │
│                                                  │
│  1. 覆盖所有云端来源的员工信息                    │
│  2. 覆盖所有云端来源的工时记录                    │
│  3. 本地新增的数据不受影响                        │
│  4. 您对云端数据的本地修改将丢失                  │
│                                                  │
│  ⚠️ 此操作不可撤销，建议先备份数据！              │
│                                                  │
│  最后同步时间：2025-11-25 10:30                   │
│                                                  │
│  [确认同步]  [取消]                               │
└──────────────────────────────────────────────────┘
```

**同步成功提示：**
```
┌──────────────────────────────────────────────────┐
│  ✅ 同步成功！                                     │
├──────────────────────────────────────────────────┤
│  📊 同步统计：                                     │
│                                                  │
│  👥 员工：                                        │
│     - 更新：5 人                                  │
│     - 新增：2 人                                  │
│                                                  │
│  📋 工时记录：                                     │
│     - 更新：15 条                                 │
│     - 新增：8 条                                  │
│                                                  │
│  同步时间：2025-11-26 14:30:22                    │
│                                                  │
│  [确定]                                          │
└──────────────────────────────────────────────────┘
```

**添加/编辑员工表单：**
```
┌─────────────────────────────────────┐
│  添加员工（本地）                    │
├─────────────────────────────────────┤
│  员工姓名：*                         │
│  [___________________]              │
│                                     │
│  工作地点：                          │
│  [___________________]              │
│                                     │
│  默认时薪（$/小时）：*               │
│  $ [___]                            │
│                                     │
│  备注：                              │
│  [___________________]              │
│                                     │
│  💡 提示：此员工仅保存在本地          │
│                                     │
│  [保存]  [取消]                      │
└─────────────────────────────────────┘
```

---

### 3️⃣ **员工工时管理页（Work Records）** - `work-records.html`

**功能：列表 + 表格形式 + 云端数据集成**

**页面布局：**
```
┌──────────────────────────────────────────────────────────────────┐
│ ← 返回        员工工时管理       [🔄 从 zmanpro 同步]  ➕ 添加   │
├──────────────────────────────────────────────────────────────────┤
│  员工筛选：[全部员工 ▼]   日期范围：[本月 ▼] [自定义...]        │
│  数据来源：[全部 ▼] [仅云端数据] [仅本地数据]                    │
├──────────────────────────────────────────────────────────────────┤
│  工时记录列表（表格形式，区分数据来源）                           │
│  ┌────┬──────┬────────┬──────┬──────┬──────┬──────┬──────────┐ │
│  │来源│员工  │ 日期   │工时  │时薪  │起止  │支出  │备注  操作│ │
│  ├────┼──────┼────────┼──────┼──────┼──────┼──────┼──────────┤ │
│  │☁️  │张三  │12/01   │8h    │$25/h │09-18 │$200  │常规 [查看]│ │ ← 云端数据（灰色背景）
│  │💻  │张三  │12/02   │10h   │$25/h │09-20 │$250  │加班 ✏️ 🗑️ │ │ ← 本地数据（正常）
│  │💻  │李四  │12/01   │6h    │$30/h │10-17 │$180  │    ✏️ 🗑️  │ │
│  │☁️  │张三  │12/03   │8h    │$25/h │08-17 │$200  │    [查看] │ │
│  │💻  │王五  │12/03   │8h    │$20/h │08-17 │$160  │    ✏️ 🗑️  │ │
│  └────┴──────┴────────┴──────┴──────┴──────┴──────┴──────────┘ │
│                                                                  │
│  📊 统计（当前筛选条件）：                                        │
│  云端数据：16h / $400  |  本地数据：24h / $590  |  总计：40h / $990 │
│                                                                  │
│  说明：☁️ 云端数据只读，无法编辑  |  💻 本地数据可编辑            │
└──────────────────────────────────────────────────────────────────┘
```

**云端工时记录详情弹窗（点击"查看"）：**
```
┌──────────────────────────────────────┐
│  ☁️ 云端工时记录（只读）              │
├──────────────────────────────────────┤
│  员工：张三                           │
│  日期：2025-12-01                     │
│  工作时长：8 小时                     │
│  时薪：$25/小时                       │
│  工作时间：09:00 - 18:00              │
│  支出：$200                           │
│  备注：常规工作日                     │
│                                      │
│  数据来源：zmanpro 系统               │
│  同步时间：2025-11-26 14:30           │
│                                      │
│  💡 此记录来自云端，无法编辑          │
│     如需修改，请在 zmanpro 系统中修改  │
│     然后点击"同步"按钮更新            │
│                                      │
│  [关闭]                               │
└──────────────────────────────────────┘
```

**添加/编辑本地工时记录表单：**
```
┌─────────────────────────────────────┐
│  添加工时记录（本地）                │
├─────────────────────────────────────┤
│  员工：*                             │
│  [选择员工 ▼]  (包含云端+本地员工)   │
│                                     │
│  日期：*                             │
│  [2025-12-01]                       │
│                                     │
│  工作时长：*                         │
│  [8] 小时                            │
│    或 时间范围：                     │
│  [09:00] - [18:00]  (自动计算)      │
│                                     │
│  时薪（$/小时）：*                   │
│  $ [25]  (默认读取员工设置)          │
│                                     │
│  支出：$200  (自动计算)              │
│                                     │
│  备注：                              │
│  [___________________]              │
│                                     │
│  💡 此记录仅保存在本地                │
│                                     │
│  [保存]  [取消]                      │
└─────────────────────────────────────┘
```

**功能特点：**
- ✅ 所有工时记录平铺显示（员工名字在第一列）
- ✅ 按员工筛选（包含云端+本地员工）
- ✅ 按日期范围筛选
- ✅ 按数据来源筛选（云端/本地/全部）
- ✅ 云端数据只读显示（灰色背景，标记☁️）
- ✅ 本地数据可编辑（正常显示，标记💻）
- ✅ 统计分别显示云端+本地+总计
- ✅ 从 zmanpro 同步（覆盖云端数据）

**数据去重逻辑：**
- 同步时，根据 `cloud_id` 判断是否已存在
- 如果已存在，更新该记录
- 如果不存在，新增该记录
- 本地数据（`source: "local"`）不受影响

---

### 4️⃣ **客户管理页（Clients）** - `clients.html`

**功能：类似员工管理**

**客户列表页：**
```
┌─────────────────────────────────────┐
│ ← 返回        客户管理        ➕ 添加 │
├─────────────────────────────────────┤
│  🔍 搜索客户...                      │
├─────────────────────────────────────┤
│  客户列表（2列布局）                  │
│  ┌──────────┐ ┌──────────┐          │
│  │ABC公司    │ │XYZ公司   │          │
│  │李经理     │ │王总      │          │
│  │本月收入:  │ │本月收入: │          │
│  │¥6,000    │ │¥3,500   │          │
│  │✏️ 🗑️      │ │✏️ 🗑️     │          │
│  └──────────┘ └──────────┘          │
└─────────────────────────────────────┘
```

**点击客户卡片 → 进入该客户的项目列表页：**
```
┌─────────────────────────────────────┐
│ ← 返回     ABC公司 - 项目    ➕ 添加 │
├─────────────────────────────────────┤
│  项目列表                            │
│  ┌─────────────────────────────────┐│
│  │ 网站开发项目                     ││
│  │ 📅 2025-12-01 ~ 2025-12-15      ││
│  │ ⏱️  15天 × 8小时 = 120小时       ││
│  │ 💰 ¥50/小时                      ││
│  │ 💵 总计：¥6,000                  ││
│  │ 📝 第一期开发                    ││
│  │ ✏️ 编辑  🗑️ 删除                 ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

**添加/编辑项目表单（支持多计费段）：**
```
┌──────────────────────────────────────────────────────────┐
│  添加客户项目                                             │
├──────────────────────────────────────────────────────────┤
│  项目名称：                                               │
│  [网站开发项目_____________________]                      │
│                                                          │
│  整体时间范围：                                           │
│  [2025-12-01] ~ [2025-12-15]  （共 15 天）               │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  📋 计费明细（可添加 1-4 条）                             │
│                                                          │
│  ▼ 计费段 1                                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 日期范围：[12/01] ~ [12/10]  （10 天）              │ │
│  │ 每天小时数：[8] 小时                                 │ │
│  │ 小时数小计：80 小时（自动计算）                       │ │
│  │ 计费单价：$ [50] /小时                               │ │
│  │ ☐ 节假日费率                                         │ │
│  │ 小计：$4,000（自动计算）                             │ │
│  │ 备注：[常规工作日___]                                │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ▼ 计费段 2                                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 日期范围：[12/11] ~ [12/15]  （5 天）               │ │
│  │ 每天小时数：[8] 小时                                 │ │
│  │ 小时数小计：40 小时（自动计算）                       │ │
│  │ 计费单价：$ [75] /小时                               │ │
│  │ ☑ 节假日费率                                         │ │
│  │ 小计：$3,000（自动计算）                             │ │
│  │ 备注：[节假日加价___]                                │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  [➕ 添加计费段]  （最多 4 个）                           │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  项目总计：                                               │
│  总天数：15 天  /  总小时数：120 小时  /  总金额：$7,000  │
│                                                          │
│  项目备注：                                               │
│  [第一期开发___________________]                          │
│                                                          │
│  [保存项目]  [取消]                                       │
└──────────────────────────────────────────────────────────┘
```

**表单逻辑：**
1. 每个项目可以添加 1-4 个计费段
2. 每个计费段可以设置不同的日期范围和计费单价
3. 支持节假日费率标记
4. 所有金额自动计算
5. 日期范围不能超出项目整体时间范围

---

### 5️⃣ **财务总结页（Summary）** - `summary.html`

**功能：核心财务分析 + 按员工/客户统计**

```
┌──────────────────────────────────────────────────────────┐
│ ← 返回        财务总结                                    │
├──────────────────────────────────────────────────────────┤
│  选择时间段：                                             │
│  [2025-12-01] ~ [2025-12-15]                             │
│  [查询]  [本周] [本月] [本季度] [今年]                    │
├──────────────────────────────────────────────────────────┤
│  📊 汇总数据（扁平卡片布局）                               │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐           │
│  │ 💰 总收入   │ │ 💸 总支出   │ │ 📈 净利润   │           │
│  │ $9,500     │ │ $5,200     │ │ $4,300     │           │
│  │ 2个客户    │ │ 3个员工    │ │ 利润率     │           │
│  │ 3个项目    │ │ 192小时    │ │ 45.3%      │           │
│  └────────────┘ └────────────┘ └────────────┘           │
├──────────────────────────────────────────────────────────┤
│  📑 标签切换：[📋 明细] [👥 按员工] [🏢 按客户]           │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  【当前选中：📋 明细】                                     │
│                                                          │
│  ▼ 收入明细                                               │
│  ┌────────────────────────────────────────────────────┐ │
│  │ ABC公司 - 网站开发项目           $7,000             │ │
│  │ XYZ公司 - APP开发项目            $2,500             │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ▼ 支出明细                                               │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 张三：88小时 × $25/h 平均        $2,200             │ │
│  │ 李四：64小时 × $30/h 平均        $1,920             │ │
│  │ 王五：40小时 × $20/h 平均        $800               │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**【按员工统计】标签页：**
```
┌──────────────────────────────────────────────────────────┐
│  📊 员工工时统计（2025-12-01 ~ 2025-12-15）               │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 排序：[按支出降序 ▼]                               │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  表格显示：                                               │
│  ┌──────┬──────────┬───────────┬──────────┬─────────┐  │
│  │排名  │ 员工     │ 总工时    │ 平均时薪 │ 总支出  │  │
│  ├──────┼──────────┼───────────┼──────────┼─────────┤  │
│  │ 🥇   │ 张三     │ 88h       │ $25/h    │ $2,200  │  │
│  │ 🥈   │ 李四     │ 64h       │ $30/h    │ $1,920  │  │
│  │ 🥉   │ 王五     │ 40h       │ $20/h    │ $800    │  │
│  └──────┴──────────┴───────────┴──────────┴─────────┘  │
│                                                          │
│  💡 提示：点击员工名可查看该员工的详细工时记录            │
└──────────────────────────────────────────────────────────┘
```

**【按客户统计】标签页：**
```
┌──────────────────────────────────────────────────────────┐
│  📊 客户收入统计（2025-12-01 ~ 2025-12-15）               │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 排序：[按收入降序 ▼]                               │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  表格显示：                                               │
│  ┌──────┬──────────┬───────────┬──────────┬─────────┐  │
│  │排名  │ 客户     │ 项目数    │ 总小时数 │ 总收入  │  │
│  ├──────┼──────────┼───────────┼──────────┼─────────┤  │
│  │ 🥇   │ ABC公司  │ 2个       │ 140h     │ $7,000  │  │
│  │ 🥈   │ XYZ公司  │ 1个       │ 50h      │ $2,500  │  │
│  │ 🥉   │ DEF公司  │ 1个       │ 30h      │ $1,500  │  │
│  └──────┴──────────┴───────────┴──────────┴─────────┘  │
│                                                          │
│  💡 提示：点击客户名可查看该客户的所有项目明细            │
└──────────────────────────────────────────────────────────┘
```

---

## 🔧 核心功能模块

### 1. localStorage 数据管理（`storage.js`）

```javascript
// 核心功能：
- saveData(key, data)          // 保存数据
- loadData(key)                // 读取数据
- exportAllData()              // 导出所有数据为JSON
- importAllData(jsonData)      // 从JSON导入数据
- clearAllData()               // 清空所有数据（需确认）

// 新增：云端同步相关
- syncFromCloud()              // 从 zmanpro 同步员工和工时数据
- getCloudSyncStatus()         // 获取最后同步时间和统计
- mergeCloudData(employees, records) // 合并云端数据到本地
```

### 2. 云端数据同步逻辑（`storage.js` + `work-records.js`）

#### 同步流程详解：

**步骤 1：用户点击"🔄 从 zmanpro 同步"按钮**
```javascript
async function handleSyncButton() {
  // 显示二次确认弹窗
  const confirmed = showConfirmDialog({
    title: '⚠️ 同步确认',
    message: `从 zmanpro 同步数据将会：
      1. 覆盖所有云端来源的员工信息
      2. 覆盖所有云端来源的工时记录
      3. 本地新增的数据不受影响
      4. 您对云端数据的本地修改将丢失
      
      ⚠️ 此操作不可撤销，建议先备份数据！`,
    buttons: ['确认同步', '取消']
  });
  
  if (!confirmed) return;
  
  // 执行同步
  await syncFromCloud();
}
```

**步骤 2：从 zmanpro API 拉取数据**
```javascript
async function syncFromCloud() {
  try {
    showLoading('正在从 zmanpro 同步数据...');
    
    // 1. 拉取所有员工
    const employeesResponse = await fetch(
      'https://zmansys-api.jerryyu077.workers.dev/api/employees'
    );
    const cloudEmployees = await employeesResponse.json();
    
    // 2. 拉取所有工时记录（可能需要分页）
    const recordsResponse = await fetch(
      'https://zmansys-api.jerryyu077.workers.dev/api/work-records/all'
    );
    const cloudRecords = await recordsResponse.json();
    
    // 3. 合并数据到本地
    const syncResult = mergeCloudData(cloudEmployees.data, cloudRecords.data);
    
    // 4. 显示同步结果
    showSyncResult(syncResult);
    
    // 5. 刷新页面
    reloadPage();
    
  } catch (error) {
    showError('同步失败：' + error.message);
  }
}
```

**步骤 3：合并数据到本地 localStorage**
```javascript
function mergeCloudData(cloudEmployees, cloudRecords) {
  // 读取本地数据
  let localEmployees = loadData('employees') || [];
  let localRecords = loadData('work_records') || [];
  
  const syncTime = new Date().toISOString();
  const stats = {
    employees: { updated: 0, added: 0 },
    records: { updated: 0, added: 0 }
  };
  
  // === 处理员工数据 ===
  
  // 1. 删除所有旧的云端员工（将被覆盖）
  localEmployees = localEmployees.filter(emp => emp.source !== 'cloud');
  
  // 2. 添加云端员工，标记来源
  cloudEmployees.forEach(cloudEmp => {
    const localEmp = {
      id: generateLocalId(),
      name: cloudEmp.name,
      location: cloudEmp.location,
      default_hourly_rate: cloudEmp.default_hourly_rate,
      notes: cloudEmp.notes,
      source: 'cloud',
      cloud_id: cloudEmp.id,
      synced_at: syncTime,
      modified_locally: false,
      editable: true,
      created_at: cloudEmp.created_at
    };
    
    localEmployees.push(localEmp);
    stats.employees.added++;
  });
  
  // 3. 保存员工数据
  saveData('employees', localEmployees);
  
  
  // === 处理工时记录 ===
  
  // 1. 删除所有旧的云端工时记录（将被覆盖）
  localRecords = localRecords.filter(rec => rec.source !== 'cloud');
  
  // 2. 添加云端工时记录，标记来源
  cloudRecords.forEach(cloudRec => {
    // 查找对应的本地员工
    const localEmp = localEmployees.find(e => e.cloud_id === cloudRec.employee_id);
    
    if (localEmp) {
      const localRec = {
        id: generateLocalId(),
        employee_id: localEmp.id,
        employee_name: localEmp.name,
        date: cloudRec.date,
        total_hours: cloudRec.total_hours,
        hourly_rate: cloudRec.hourly_rate,
        start_time: cloudRec.start_time,
        end_time: cloudRec.end_time,
        notes: cloudRec.notes,
        total_amount: cloudRec.total_hours * cloudRec.hourly_rate,
        source: 'cloud',
        cloud_id: cloudRec.id,
        cloud_employee_id: cloudRec.employee_id,
        synced_at: syncTime,
        editable: false,
        display_style: 'readonly',
        created_at: cloudRec.created_at
      };
      
      localRecords.push(localRec);
      stats.records.added++;
    }
  });
  
  // 3. 保存工时记录
  saveData('work_records', localRecords);
  
  // 4. 保存同步时间
  saveData('last_sync_time', syncTime);
  
  return stats;
}
```

**步骤 4：显示同步结果**
```javascript
function showSyncResult(stats) {
  showDialog({
    title: '✅ 同步成功！',
    content: `
      📊 同步统计：
      
      👥 员工：
         - 更新：${stats.employees.updated} 人
         - 新增：${stats.employees.added} 人
      
      📋 工时记录：
         - 更新：${stats.records.updated} 条
         - 新增：${stats.records.added} 条
      
      同步时间：${new Date().toLocaleString('zh-CN')}
    `,
    buttons: ['确定']
  });
}
```

---

### 3. 自动计算逻辑

#### 员工支出计算（合并云端+本地数据）：
```javascript
// 单日支出
total_amount = total_hours × hourly_rate

// 时间段支出（包含云端和本地数据）
period_expense = SUM(
  所有工时记录在时间段内的 total_amount
  // source 为 "cloud" 或 "local" 都计入
)

// 按员工统计支出
employee_expense = SUM(
  该员工的所有工时记录（云端+本地）
)
```

#### 客户收入计算：
```javascript
// 单个项目收入
total_hours = days_count × hours_per_day
total_amount = total_hours × hourly_charge

// 时间段收入（需要考虑项目时间段重叠）
period_income = SUM(项目时间段与查询时间段有重叠的 total_amount)
```

#### 利润计算：
```javascript
profit = period_income - period_expense
profit_margin = (profit / period_income) × 100%
```

### 3. 日期范围查询逻辑

**员工支出查询：**
- 按 `work_records.date` 在 `[start_date, end_date]` 范围内筛选

**客户收入查询：**
- 项目时间段与查询时间段有重叠即计入
- 重叠判断：`project.end_date >= query.start_date AND project.start_date <= query.end_date`

---

## 📦 数据管理页（Data Management）- `data-management.html`

### 6️⃣ **数据管理页**

**页面布局：**
```
┌──────────────────────────────────────────────────────────┐
│ ← 返回        数据管理                                    │
├──────────────────────────────────────────────────────────┤
│  📊 数据统计                                              │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐          │
│  │ 👥 员工     │ │ 📋 工时记录 │ │ 🏢 客户     │          │
│  │ 12 人      │ │ 358 条     │ │ 8 家       │          │
│  └────────────┘ └────────────┘ └────────────┘          │
│  ┌────────────┐                                         │
│  │ 💰 客户项目 │                                         │
│  │ 25 个      │                                         │
│  └────────────┘                                         │
├──────────────────────────────────────────────────────────┤
│  💾 数据备份                                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 定期备份数据可以防止数据丢失                        │ │
│  │                                                    │ │
│  │ 上次备份：2025-11-25 14:30                         │ │
│  │                                                    │ │
│  │ [📥 导出全部数据]                                   │ │
│  │                                                    │ │
│  │ 文件格式：JSON                                      │ │
│  │ 文件名：finance_backup_20251126_143022.json       │ │
│  └────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────┤
│  📤 数据导入                                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │ ⚠️ 导入数据将覆盖当前所有数据，请谨慎操作！         │ │
│  │                                                    │ │
│  │ [📁 选择备份文件]                                   │ │
│  │                                                    │ │
│  │ 导入选项：                                          │ │
│  │ ⚪ 完全覆盖（清空现有数据后导入）                   │ │
│  │ ⚪ 智能合并（保留现有数据，追加新数据）             │ │
│  │                                                    │ │
│  │ [确认导入]  [取消]                                  │ │
│  └────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────┤
│  🗑️ 危险操作                                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │ [清空所有数据]                                      │ │
│  │                                                    │ │
│  │ ⚠️ 此操作不可撤销！请先备份数据！                   │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 备份功能详细流程
```javascript
// 点击"导出全部数据"按钮
1. 读取所有 localStorage 数据：
   - employees（员工）
   - work_records（工时记录）
   - clients（客户）
   - client_projects（客户项目）
   
2. 生成 JSON 对象：
   {
     "version": "1.0",
     "export_date": "2025-11-26T14:30:22Z",
     "data": {
       "employees": [...],
       "work_records": [...],
       "clients": [...],
       "client_projects": [...]
     }
   }
   
3. 生成文件名：finance_backup_YYYYMMDD_HHMMSS.json

4. 触发浏览器下载

5. 记录备份时间到 localStorage
```

### 导入功能详细流程
```javascript
// 点击"选择备份文件" → 选择文件 → 点击"确认导入"

1. 读取 JSON 文件

2. 验证数据格式：
   - 检查 version 字段
   - 检查必要的数据表是否存在
   - 验证数据结构

3. 根据用户选择的导入模式：
   
   【完全覆盖模式】
   - 清空所有 localStorage 数据
   - 写入新数据
   
   【智能合并模式】
   - 检查 ID 冲突
   - 保留现有数据
   - 追加新数据（使用新 ID）

4. 显示导入结果：
   - 成功导入 X 个员工
   - 成功导入 X 条工时记录
   - 成功导入 X 个客户
   - 成功导入 X 个项目

5. 刷新页面或返回首页
```

---

## ✅ 用户确认的设计决策

### 1. ✅ 货币单位
- **全部使用美元 $** 进行结算和显示

### 2. ✅ 客户项目收入计算方式
- **方案 A：按项目总额计算**
- 每个 period（时间段）可有 1-4 条 input
- **支持节假日特殊计费费率**（holiday special charge rate）
- 需要支持同一项目不同天的不同计费

### 3. ✅ 员工工时页面
- **简化为列表界面**（不再使用日历）
- **不需要点击进入员工详情**
- 直接在列表中把员工名字放在第一列
- 所有工时记录平铺显示，可按员工筛选

### 4. ✅ 统计维度
**基础统计：**
- 按时间段统计总收入/支出/利润

**新增统计：**
- ✅ 按员工统计（每个员工的总工时、平均时薪、总支出、排名）
- ✅ 按客户统计（每个客户的项目数、总小时数、总收入、排名）

**统计特点：**
- 支持排序（按金额、按工时）
- 支持点击查看详情
- 自动计算平均值

### 5. ✅ UI 风格
- **扁平风格**（Flat Design）
- 更适合数据展示
- 简洁清爽的表格/卡片布局

### 6. ✅ 云端数据同步（zmanpro 集成）
**同步范围：**
- ✅ 同步员工信息（姓名、地点、时薪）
- ✅ 同步工时记录（只读显示）

**同步行为：**
- ✅ 完全覆盖（点击"同步"会用云端数据覆盖本地）
- ✅ 二次确认（弹窗警告，避免误操作）

**数据来源标记：**
- ☁️ 云端数据（从 zmanpro 同步，只读，灰色显示）
- 💻 本地数据（本地新增/编辑，可修改，正常显示）

**客户数据：**
- ❌ 不从云端同步，完全本地管理

**核心逻辑：**
- 员工和工时数据可从 `zmansys-api.jerryyu077.workers.dev` 同步
- 本地可新增/编辑员工和工时（标记为本地数据）
- 本地修改不会上传到云端
- 同步操作会覆盖本地修改（需二次确认）
- 财务计算时合并云端+本地数据

---

## 🎨 UI 设计风格 - 扁平风格（Flat Design）

### 设计原则
```css
/* 扁平风格核心特征 */
- 纯色背景（无渐变）
- 清晰的边框分隔
- 简洁的卡片布局
- 充足的留白
- 清晰的表格线条
- 无阴影或极浅阴影
- 高对比度的文字
- 明确的视觉层级
```

### 配色方案（建议）
```
主色调：
- 主背景：#F5F7FA（浅灰蓝）
- 卡片背景：#FFFFFF（纯白）
- 边框：#E1E8ED（浅灰）

强调色：
- 收入/成功：#10B981（绿色）
- 支出/警告：#EF4444（红色）
- 利润/信息：#3B82F6（蓝色）
- 节假日：#F59E0B（橙色）

文字：
- 主文字：#1F2937（深灰）
- 次要文字：#6B7280（中灰）
- 占位符：#9CA3AF（浅灰）
```

### 布局特点
- 宽松的内边距（padding: 16px~24px）
- 清晰的模块分隔
- 响应式表格
- 简洁的按钮（纯色填充或边框按钮）

---

## 🎯 下一步计划

**✅ 设计方案已确认，包括：**

1. ✅ 货币单位：美元 $
2. ✅ 数据结构：支持多计费段、节假日费率
3. ✅ 页面布局：扁平风格、列表化、表格化
4. ✅ 统计功能：按员工、按客户
5. ✅ 计算逻辑：项目总额、不同天不同计费

**🚀 准备开始编写代码**

---

## 🔌 zmanpro API 集成要求

### 需要的 API 端点

**现有端点（已可用）：**
```
✅ GET /api/employees
   - 获取所有员工列表
   - 返回：{ success: true, data: [...] }

✅ GET /api/work-records?employee_id={id}
   - 获取指定员工的工时记录
   - 返回：{ success: true, data: [...] }
```

**新增端点（需要添加）：**
```
⚠️ GET /api/work-records/all
   - 获取所有员工的所有工时记录
   - 返回：{ success: true, data: [...] }
   - 用途：一次性拉取所有工时记录进行同步
   
   示例实现（添加到 worker/index.js）：
   
   if (url.pathname === '/api/work-records/all' && request.method === 'GET') {
     const { results } = await env.DB.prepare(`
       SELECT id, employee_id, date, total_hours, hourly_rate, 
              start_time, end_time, notes, created_at
       FROM work_records
       ORDER BY date DESC, created_at DESC
     `).all();
     
     return jsonResponse({ success: true, data: results });
   }
```

### CORS 配置
确保 Worker 允许跨域访问：
```javascript
// worker/index.js 中已有的 CORS 配置
const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type',
};
```

### 数据格式要求
云端 API 返回的数据格式需要包含以下字段：

**员工（employees）：**
```json
{
  "id": 2,
  "name": "张三",
  "location": "北京",
  "default_hourly_rate": 25,
  "notes": "全职员工",
  "created_at": "2025-11-22 00:36:09"
}
```

**工时记录（work_records）：**
```json
{
  "id": 15,
  "employee_id": 2,
  "date": "2025-12-01",
  "total_hours": 8,
  "hourly_rate": 25,
  "start_time": "09:00",
  "end_time": "18:00",
  "notes": "常规工作日",
  "created_at": "2025-12-01 18:00:00"
}
```

---

## 💡 技术实现要点

### 无需任何依赖
```html
<!-- 完全独立的文件 -->
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <!-- 页面内容 -->
  <script src="js/storage.js"></script>
  <script src="js/utils.js"></script>
  <script src="js/app.js"></script>
</body>
</html>
```

### 直接双击打开
- 所有路径使用相对路径
- 不依赖任何 CDN（所有代码都在本地）
- 兼容 `file://` 协议

### localStorage 容量
- 现代浏览器：5-10MB
- 估算：1000 条员工工时记录 + 500 个客户项目 ≈ 500KB
- **完全够用** ✅

---

## 📝 开发实施清单

### 第一阶段：基础框架（1-2小时）
- [ ] 创建项目文件夹结构
- [ ] 编写 `flat-style.css`（扁平风格）
- [ ] 编写 `storage.js`（localStorage 封装）
- [ ] 编写 `utils.js`（工具函数）
- [ ] 创建 `index.html`（主导航页）

### 第二阶段：员工模块 + 云端同步（3-4小时）
- [ ] 创建 `employee-management.html`（员工基础管理 + 同步按钮）
- [ ] 编写 `employee-mgmt.js`（员工CRUD逻辑）
- [ ] 创建 `work-records.html`（工时记录管理 + 数据来源区分）
- [ ] 编写 `work-records.js`（工时记录逻辑）
- [ ] 实现 `syncFromCloud()` 函数（核心同步逻辑）
- [ ] 实现二次确认弹窗
- [ ] 实现数据来源标记（云端☁️ vs 本地💻）
- [ ] 实现云端数据只读逻辑

### 第三阶段：客户模块（3-4小时）
- [ ] 创建 `clients.html`（客户管理）
- [ ] 编写 `clients.js`（客户和项目逻辑）
- [ ] 实现多计费段输入表单
- [ ] 实现节假日费率标记

### 第四阶段：财务总结（2-3小时）
- [ ] 创建 `summary.html`（财务总结页）
- [ ] 编写 `summary.js`（统计计算逻辑）
- [ ] 实现按时间段筛选
- [ ] 实现按员工统计
- [ ] 实现按客户统计

### 第五阶段：数据管理（1-2小时）
- [ ] 创建 `data-management.html`（数据管理页）
- [ ] 编写 `data-mgmt.js`（备份/导入逻辑）
- [ ] 实现数据导出功能
- [ ] 实现数据导入功能

### 第六阶段：优化和测试（1-2小时）
- [ ] 完善所有页面的响应式布局
- [ ] 添加数据验证和错误提示
- [ ] 测试所有功能
- [ ] 修复 bug

**预计总开发时间：12-18 小时**（增加了云端同步功能）

---

## ✅ 设计方案总结

**✅ 完全可行！所有设计决策已确认：**

1. ✅ **货币单位**：美元 $
2. ✅ **收入计算**：按项目总额，支持1-4个计费段，支持节假日费率
3. ✅ **员工工时**：简化为列表/表格，不再使用日历界面
4. ✅ **统计维度**：基础统计 + 按员工 + 按客户
5. ✅ **UI 风格**：扁平风格（Flat Design）
6. ✅ **云端集成**：从 zmanpro 同步员工和工时数据

**技术特点：**
- ✅ 纯前端技术（HTML + CSS + JavaScript）
- ✅ 零配置启动（双击 HTML 即可使用）
- ✅ 数据本地存储（localStorage，5-10MB容量）
- ✅ 云端数据同步（从 zmanpro API 拉取数据）
- ✅ 混合数据管理（云端只读 + 本地可编辑）
- ✅ 完整的数据备份/导入功能
- ✅ 功能逻辑清晰，代码结构良好

**核心创新点：**
- 🔄 云端同步按钮，一键拉取 zmanpro 数据
- ☁️ 云端数据标记（灰色背景，只读）
- 💻 本地数据标记（正常显示，可编辑）
- 📊 财务计算合并云端+本地数据
- ⚠️ 二次确认机制，防止误操作

**准备开始编码！🚀**

---

## ❓ 常见问题（FAQ）

### Q1: 云端数据和本地数据有什么区别？
**A:** 
- **☁️ 云端数据**：从 zmanpro 系统同步过来的数据，显示为灰色背景，只能查看不能编辑
- **💻 本地数据**：在本地财务系统中新增的数据，正常显示，可以编辑和删除
- **财务计算**：两种数据都会参与计算

### Q2: 如果我修改了云端数据，然后点击同步会怎样？
**A:** 云端数据的修改会丢失。点击同步时会弹出警告，确认后会用云端最新数据覆盖本地的云端数据。**建议**：不要修改云端数据，只修改本地数据。

### Q3: 本地新增的员工和工时会同步到 zmanpro 吗？
**A:** **不会**。本地数据只保存在浏览器中，不会上传到云端。如果需要在 zmanpro 中管理这些数据，需要手动在 zmanpro 系统中添加。

### Q4: 我可以在不同浏览器或设备上使用吗？
**A:** 
- **云端数据**：可以，每次点击同步都会拉取最新的 zmanpro 数据
- **本地数据**：不可以，localStorage 只在单个浏览器中有效
- **建议**：定期使用"数据导出"功能备份本地数据

### Q5: 如果我清除了浏览器数据会怎样？
**A:** 
- **云端数据**：可以通过"同步"按钮重新拉取
- **本地数据**：会永久丢失（除非有备份）
- **建议**：定期导出备份数据

### Q6: 客户和项目数据可以从云端同步吗？
**A:** **不可以**。zmanpro 系统目前没有客户和项目数据表，所以客户管理（收入模块）完全在本地管理。

### Q7: 同步会删除我的本地数据吗？
**A:** **不会**。同步只会覆盖云端来源的数据，本地来源（`source: "local"`）的数据不受影响。

### Q8: 如何区分某条工时记录是云端还是本地？
**A:** 在工时记录列表中：
- 云端数据：第一列显示 ☁️，灰色背景，操作栏只有"查看"按钮
- 本地数据：第一列显示 💻，正常显示，操作栏有"编辑"和"删除"按钮

### Q9: 可以把本地数据标记为云端数据吗？
**A:** **不建议**。数据来源标记是自动管理的，手动修改可能导致数据混乱。如果需要，应该在 zmanpro 系统中添加数据，然后通过同步获取。

### Q10: 同步功能需要网络连接吗？
**A:** **需要**。同步时需要连接到 `zmansys-api.jerryyu077.workers.dev`。如果没有网络，可以正常使用本地功能，只是无法同步云端数据。

